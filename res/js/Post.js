class Post {
    constructor(authorFirstname, authotLastname, authorAvatar, createTime, text, media, likes) {
        this.authorFirstname = authorFirstname;
        this.authorLastname = authorLastname;
        this.authorAvatar = authorAvatar;
        this.createTime = createTime;
        this.text = text;
        this.media = media;
        this.likes = likes;
    }
}